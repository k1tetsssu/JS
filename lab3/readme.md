# Лабораторная работа №3. Продвинутые объекты в JavaScript

## Цель работы

Познакомиться с классами и объектами в JavaScript, научиться:
- создавать пользовательские классы,
- использовать конструкторы и методы,
- реализовать наследование между классами.

## Условие

Создать консольное приложение, моделирующее систему инвентаря, где можно:
- добавлять предметы,
- изменять их свойства,
- управлять ими через методы классов.

---

## Шаг 1. Создание класса `Item`

Создал базовый класс `Item`, представляющий предмет инвентаря.

### Поля класса:
- `name` — название предмета;
- `weight` — вес предмета (число ≥ 0);
- `rarity` — редкость предмета:  
  `common`, `uncommon`, `rare`, `legendary`.

### Методы:
- `getInfo()` — возвращает строку с описанием предмета;
- `setWeight(newWeight)` — изменяет вес предмета.

### Пример использования:
```javascript
//Выводим информацию о предмете
    const sword = new Item("Steel Sword", 3.5, "rare");
    console.log(sword.getInfo()); // Предмет: Steel Sword, Вес: 3.5, Редкость: Rare
```

```javascript
// Изменяем вес
    sword.setWeight(4.0);
    console.log(`Текущий вес меча: ${sword.weight.toFixed(1)}`);
    console.log(sword.getInfo()); // Предмет: Steel Sword, Вес: 4.0 , Редкость: Rare
```
---

## Шаг 2. Создание класса `Weapon`

Класс `Weapon` расширяет `Item` и добавляет функциональность оружия.

### Дополнительные поля:
- `damage` — урон оружия (целое число);
- `durability` — прочность (от 0 до 100).

### Методы:
- `use()` — уменьшает `durability` на 10, но не ниже 0;
- `repair()` — восстанавливает `durability` до 100;
- `getInfo()` — переопределён для отображения урона и прочности.

### Пример использования:
```javascript
const bow = new Weapon("Longbow", 2.0, "uncommon", 15, 100);
console.log(bow.getInfo());       // Предмет: Longbow, Вес: 2.0, Редкость: Uncommon, Урон: 15, Прочность: 100%
bow.use();                        // Прочность уменьшается на 10
console.log(bow.durability);      // 90
bow.repair();                     // Прочность восстанавливается до 100
```

---

## Шаг 3. Тестирование

Были созданы и протестированы следующие объекты:

### Объекты класса `Item`:
- `Steel Sword` — с редкостью `rare`, вес изменён с 3.5 до 4.
0;

  [Пример](#пример-использования)

- `Health Potion` — простой предмет с редкостью `common`.

```javascript
// Пример создания другого предмета
    const potion = new Item("Health Potion", 0.5, "common");
    console.log(potion.getInfo()); //Предмет: Health Potion , Вес: 0.5 , Редкость: common
```
- `Dimond Sword` - предмет с неправильной редкостью `minecraft`

```javascript
   // Пример ошибки при создании предмета (неправильная редкость)
    const invalidItem = new Item("Dimond Sword", 7.5, "minecraft");
    console.log(invalidItem.getInfo()); //Ошибка создания предмета: Редкость должна быть одной из: common, uncommon, rare, legendary 
```

### Объекты класса `Weapon`:
- `Longbow` — протестировано многократное использование и ремонт;

```javascript
// Создание оружия
    const bow = new Weapon("Longbow", 2.0, "uncommon", 15, 100);
    console.log(bow.getInfo()); // Предмет: Longbow, Вес: 2.0, Редкость: Uncommon, Урон: 15, Прочность: 100%

    // Используем лук несколько раз
    bow.use(); // Использован Longbow. Прочность уменшилась до 90%.
    console.log(`Прочность лука после использования: ${bow.durability}%`); // 90%
    bow.use(); 
    bow.use();
    console.log(bow.getInfo()); // Прочность: 70%

    // Ремонтируем лук
    bow.repair();
    console.log(`Прочность лука после ремонта: ${bow.durability}%`); // Прочность лука после ремонта: 100%
    bow.repair(); // Попытка починить уже целый: Longbow предмет не нуждается в ремонте.

    // Используем лук до поломки
    console.log("\nИспользуем лук до поломки...");
    for (let i = 0; i < 11; i++) { // 10 использований до 0%, 11-е - попытка использовать сломанный
        if (bow.durability > 0) {
             bow.use();
        } else {
             bow.use(); // Longbow сломан и не может быть использован.
             break;
        }
    }

    console.log(bow.getInfo()); // Предмет: Longbow, Вес: 2.0, Редкость: Uncommon, Урон: 15, Прочность: 0%
    bow.repair(); // Чиним сломанный
    console.log(bow.getInfo()); // Прочность: 100%
```
- `Battle Axe` — проверены базовые методы и уменьшение прочности.

---

## Шаг 4. Переписать с использованием функций-конструкторов и опциональной цепочки

[Код](/newScript.js)

Пример использования

```javascript
  const staff = new WeaponConstructor("Magic Staff", 1.8, "rare", 25, 100);
  console.log(staff?.getInfo());  // Предмет: Magic Staff, Вес: 1.8, Редкость: rare, Урон: 25, Прочность: 100



  staff?.use();
  console.log(staff?.durability); // 90
  staff?.repair();
  console.log(staff?.getInfo()) // Предмет: Magic Staff, Вес: 1.8, Редкость: rare, Урон: 25, Прочность: 100
```
## Контрольные вопросы
 1. Какое значение имеет this в методах класса?

 В методах класса значение this указывает на текущий экземпляр объекта, созданного с помощью этого класса. Это означает, что внутри метода this позволяет получить доступ к свойствам и другим методам того объекта, который вызвал данный метод. Например, если у нас есть объект potion, созданный из класса Item, то при вызове potion.getInfo() значение this в методе getInfo будет указывать на potion.

 2. Как работает модификатор доступа # в JavaScript?

Модификатор доступа # в JavaScript используется для создания приватных полей и методов внутри классов. Такие поля не видны и не доступны извне, они могут использоваться только внутри самого класса. Это позволяет инкапсулировать данные и защитить их от прямого изменения. Например, если в классе указать #secretCode = 12345, то попытка обратиться к object.#secretCode вне класса приведёт к ошибке.

3. В чем разница между классами и функциями-конструкторами?

Основное различие между классами и функциями-конструкторами заключается в синтаксисе и удобстве использования. Классы (class) были введены в ES6 и представляют собой более современный и читаемый способ создания объектов и реализации наследования. Они поддерживают такие возможности, как ключевое слово extends для наследования, super() для обращения к родительскому классу, а также приватные поля (#). Функции-конструкторы — это более старый подход, при котором объект создаётся через функцию с использованием ключевого слова new, а наследование реализуется через Object.create() и call. Хотя классы являются синтаксическим сахаром над функциями-конструкторами, они делают код чище, понятнее и лучше поддерживаются современными инструментами разработки.

## Вывод

В ходе лабораторной работы:
- реализована система классов с наследованием;
- изучены основы ООП в JavaScript;
- отработана работа с методами и проверкой данных;
- успешно протестирована функциональность через консоль.
